content/parsed/
content/ledger.sqlite
content/library.bib
content/provenance/
content/docling/
content/docling_cache.json
content/chroma/
content/topics.json
content/topic_embed_cache.json
content/retrieval_index.json
content/rendered/
content/drafts/
content/provenance/
__pycache__/
*.pyc
.venv/
.venv-full/
venv/
env/
.pytest_cache/
.mypy_cache/
.coverage
htmlcov/
*.egg-info/
*.log
*.tmp
*.swp
.DS_Store
release/
# papers/bibliography.bib is a manual, per-host export from a personal
# reference manager (see AGENTS.md) -- not shippable source, so it's
# gitignored rather than tracked. papers/pdfs/ is the [source_pdfs].dir
# default in config.toml (migrated 2026-07-31 from source-pdfs/, now
# retired); manifest.json/reading-notes.md there are hand-written and
# tracked, but raw PDFs dropped alongside them are not.
papers/bibliography.bib
papers/pdfs/*
!papers/pdfs/manifest.json
!papers/pdfs/reading-notes.md

# bench/ work lists are generated by bench/make_corpus.py and carry
# absolute paths into papers/, so they're per-host data for the same
# reason the bib file above is. bench/results/*.jsonl is deliberately NOT
# ignored: those are citekeys and durations, no paths, and they're the
# evidence bench/RESULTS.md rests on.
bench/corpus.json
bench/sample*.json
bench/par_*/
