# Heavier optional stack -- only installed inside docker/Dockerfile, never
# on the bare host (PEP 668 / externally-managed-environment blocks system
# pip there; use this file inside a venv/container instead).
#
# Pin exact versions once you've validated a combination that works, per
# the reproducibility guidance from the original design discussion -- the
# ranges below are a starting point, not a tested lockfile.
sentence-transformers>=3.0,<4.0
chromadb>=0.5,<0.6
bertopic>=0.16,<0.17
bibtexparser>=1.4,<2.0
